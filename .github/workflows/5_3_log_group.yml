name: Log group
on: push
jobs:
 log:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::group::Show environment variables"  # ロググループの開始
          printenv
          echo "::endgroup::"                 # ロググループの終了
